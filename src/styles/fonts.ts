import { Courier_Prime } from 'next/font/google'
import { DM_Serif_Display } from 'next/font/google'
import { Alike} from 'next/font/google'
import { Instrument_Serif } from 'next/font/google'

export const dmSerif = DM_Serif_Display({weight: ['400'], style: ['normal', 'italic'], subsets: ['latin']})
export const courier = Courier_Prime({weight: ['400'], style: ['normal', 'italic'], subsets: ['latin']})
export const marcellus = Alike({weight: ['400'], subsets: ['latin']})
export const instrumentSerif = Instrument_Serif({weight: ['400'], style: ['normal', 'italic'], subsets: ['latin']})